x-mongo-depends: &mongo
  depends_on:
    - mongo

x-test-defaults: &test
  volumes:
    - ./test/fake:/fake
  depends_on:
    - mongo

services:

  service-b:
    <<: *mongo

  test:
    <<: *test

  test-runner:
    <<: *test

  mongo:
    image: mongo:4.4.6
