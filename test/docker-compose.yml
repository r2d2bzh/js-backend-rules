# DO NOT EDIT THIS FILE AS IT IS GENERATED BY @r2d2bzh/js-backend-rules
# To customize the registry path of the released images, the following
# setting is available in the package.json file of the root project:
# { "r2d2bzh": { "dockerRegistry": ... } }
#
# The default path from where volumes are mounted is "." (the compose file path)
# This can also be customized in the package.json file of the root project:
# { "r2d2bzh": { "volumeSourceRoot": ... } }
#
# If you need to customize the compose configuration any further,
# please use docker-compose.override.yml
#
# More details are available here:
# - https://docs.docker.com/compose/reference/#specifying-multiple-compose-files
# - https://docs.docker.com/compose/extends/#adding-and-overriding-configuration
# - https://github.com/compose-spec/compose-spec/blob/master/spec.md
services:
  share:
    image: test-share:${VERSION:-dev}
    build:
      context: ./share
    profiles:
      - share
  service-a:
    build: &ref_0
      context: ./dev
      args:
        UID: ${UID}
        GID: ${GID}
        DOCKER_BUILD_NODEJS_VERSION: ${DOCKER_BUILD_NODEJS_VERSION}
    depends_on:
      - nats
    volumes:
      - ${WORKSPACE:-.}/service-a:/home/user/dev
      - ${WORKSPACE:-.}/share:/home/user/share
    command:
      - npm
      - start
    ports:
      - 9229
  service-a.rel:
    image: test-registry:9999/fake/r2d2bzh/js-backend-rules/service-a:${VERSION:-dev}
    build:
      context: ./service-a
      args:
        DOCKER_BUILD_NODEJS_VERSION: ${DOCKER_BUILD_NODEJS_VERSION}
        SHARE: test-share:${VERSION:-dev}
    depends_on:
      - nats
    profiles:
      - rel
  service-b:
    build: *ref_0
    depends_on:
      - nats
    volumes:
      - ${WORKSPACE:-.}/service-b:/home/user/dev
      - ${WORKSPACE:-.}/share:/home/user/share
    command:
      - npm
      - start
    ports:
      - 9229
  service-b.rel:
    image: test-registry:9999/fake/r2d2bzh/js-backend-rules/service-b:${VERSION:-dev}
    build:
      context: ./service-b
      args:
        DOCKER_BUILD_NODEJS_VERSION: ${DOCKER_BUILD_NODEJS_VERSION}
        SHARE: test-share:${VERSION:-dev}
    depends_on:
      - nats
    profiles:
      - rel
  service-c:
    build: *ref_0
    depends_on:
      - nats
    volumes:
      - ${WORKSPACE:-.}/service-c:/home/user/dev
      - ${WORKSPACE:-.}/share:/home/user/share
    command:
      - npm
      - start
    ports:
      - 9229
  service-c.rel:
    image: test-registry:9999/fake/r2d2bzh/js-backend-rules/service-c:${VERSION:-dev}
    build:
      context: ./service-c
      args:
        DOCKER_BUILD_NODEJS_VERSION: ${DOCKER_BUILD_NODEJS_VERSION}
        SHARE: test-share:${VERSION:-dev}
    depends_on:
      - nats
    profiles:
      - rel
  service-d:
    build: *ref_0
    depends_on:
      - nats
    volumes:
      - ${WORKSPACE:-.}/service-d:/home/user/dev
      - ${WORKSPACE:-.}/share:/home/user/share
    command:
      - npm
      - start
    ports:
      - 9229
  service-d.rel:
    image: test-registry:9999/fake/r2d2bzh/js-backend-rules/service-d:${VERSION:-dev}
    build:
      context: ./service-d
      args:
        DOCKER_BUILD_NODEJS_VERSION: ${DOCKER_BUILD_NODEJS_VERSION}
        SHARE: test-share:${VERSION:-dev}
    depends_on:
      - nats
    profiles:
      - rel
  service-e:
    build: *ref_0
    depends_on:
      - nats
    volumes:
      - ${WORKSPACE:-.}/service-e:/home/user/dev
      - ${WORKSPACE:-.}/share:/home/user/share
    command:
      - npm
      - start
    ports:
      - 9229
  service-e.rel:
    image: test-registry:9999/fake/r2d2bzh/js-backend-rules/service-e:${VERSION:-dev}
    build:
      context: ./service-e
      args:
        DOCKER_BUILD_NODEJS_VERSION: ${DOCKER_BUILD_NODEJS_VERSION}
        SHARE: test-share:${VERSION:-dev}
    depends_on:
      - nats
    profiles:
      - rel
  test:
    build: *ref_0
    volumes: &ref_1
      - ${WORKSPACE:-.}/test:/home/user/dev
      - ${WORKSPACE:-.}/share:/home/user/share
      - ${WORKSPACE:-.}/service-a:/home/user/service-a
      - ${WORKSPACE:-.}/service-b:/home/user/service-b
      - ${WORKSPACE:-.}/service-c:/home/user/service-c
      - ${WORKSPACE:-.}/service-d:/home/user/service-d
      - ${WORKSPACE:-.}/service-e:/home/user/service-e
    profiles:
      - test
    entrypoint:
      - npm
      - run
    command:
      - cov
  _test:
    build: *ref_0
    volumes: *ref_1
    ports:
      - 9229
  nats:
    image: nats:2.8.4-alpine3.15
