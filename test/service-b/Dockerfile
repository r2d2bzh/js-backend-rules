# DO NOT EDIT THIS FILE AS IT IS GENERATED BY @r2d2bzh/js-backend-rules
# The registry where docker-build-nodejs images are located can be set from
# { "dockerBuildNodejs": { "registry": ... } } in the package.json file of the root project
# For additional commands, add { "dockerBuildNodejs": { "dockerfileCommands": [ ... ] } }
# in service-b/package.json
ARG DOCKER_BUILD_NODEJS_VERSION
FROM https://test-registry/fake/docker-build-nodejs-builder:${DOCKER_BUILD_NODEJS_VERSION} as builder
FROM https://test-registry/fake/docker-build-nodejs-runtime:${DOCKER_BUILD_NODEJS_VERSION}
COPY fake /fake
RUN ls /
